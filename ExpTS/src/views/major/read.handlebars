
<div class="d-flex justify-content-between">
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{major.name}}</h5>
    <h6 class="card-subtitle mb-2 text-body-secondary">{{major.code}}</h6>
    <p class="card-text">{{major.description}}</p>
    <a href="/major/update/{{major.id}}" class="card-link">Editar</a>
    <form action="/major/remove/{{major.id}}" method="post" style="display:inline;">
        <button type="submit" class="btn btn-danger card-link">Deletar</button>
    </form>
  </div>
</div>
<div><a href="/major" class="btn btn-primary">Voltar</a></div>
</div>